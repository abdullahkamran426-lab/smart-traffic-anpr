# Core dependencies for Streamlit Cloud
streamlit==1.35.0
ultralytics==8.2.0
opencv-python-headless==4.9.0.80
numpy>=1.23
pandas>=1.5
plotly==5.22.0
pillow==10.0.0

# ANPR dependencies (CPU-only)
easyocr==1.7.1
torch
torchvision

# Utilities
python-dotenv==1.0.1
requests==2.31.0

# Note: boxmot removed - using simpler tracking for cloud
# Note: pytesseract removed - using EasyOCR only
# Note: FastAPI/uvicorn removed - not needed for Streamlit Cloud
